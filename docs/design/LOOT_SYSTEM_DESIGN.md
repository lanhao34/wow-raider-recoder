# 装备掉落记录与装备分配记录 · 设计说明（含在线登录）

本文档描述如何实现：**装备掉落记录**、**装备分配记录**，以及**在线登录 + 服务端持久化**，并贴合游戏的**普通 / 英雄 / 史诗**三难度与**弹性掉落**规则。

---

## 一、游戏规则抽象（你的需求对应）

| 难度 | 人数 | 掉落件数规则 | 说明 |
|------|------|----------------|------|
| **普通** | 弹性 | 与参与击杀人数相关 | 弹性掉落，需记录「本次击杀参与人数」再算件数 |
| **英雄** | 弹性 | 与参与击杀人数相关 | 同上 |
| **史诗** | 固定 20 人 | 固定 4 件/ Boss（额外掉落另算） | 固定掉落，可再支持“额外掉落”条数 |

- **CD 周期**：按「周」区分。同一 Boss 同一难度每周可击杀一次并获得该难度的掉落；不同难度独立（同一周可打普通 + 英雄 + 史诗各一次）。
- **记录目标**：  
  - **掉落记录**：某周、某副本、某 Boss、某难度下，本次击杀了、掉了几件、分别是什么装备。  
  - **分配记录**：某件掉落分配给了哪位成员（谁拿了这个装备）。

---

## 二、核心概念与数据模型

### 2.1 概念名词

- **周（Week）**：用「周起始日」或「周 ID」表示，例如 `2025-W12` 或时间戳，用于判断是否同一 CD。
- **Boss 击杀（RaidKill / BossKill）**：某周、某副本、某 Boss、某难度的一次击杀记录；用来挂「本次掉落的 N 件装备」。
- **掉落记录（Drop）**：某次 Boss 击杀产生的**单件**装备掉落（哪件装备、来自哪次击杀）。
- **分配记录（Distribution）**：某件掉落**分配给哪位成员**（成员 ID、分配时间、操作人等）。

### 2.2 掉落件数规则（可配置）

建议在系统里做成**可配置**，便于以后跟随游戏改动：

- **普通 / 英雄（弹性）**  
  - 输入：本次 Boss 击杀的**参与人数** `participantCount`。  
  - 输出：本次击杀的**掉落件数** `dropCount`。  
  - 实现方式二选一（或同时支持）：  
    - **公式**：例如 `dropCount = max(1, floor(participantCount / 5))`，系数可配。  
    - **查表**：例如 10 人→2 件，15 人→3 件，20 人→4 件等，表可配置。
- **史诗**  
  - 固定：人数=20，掉落件数=4（可再加“额外掉落”字段，例如 +0/+1/+2 件，由团长录入）。

这样「装备掉落记录」的条数就和「本次击杀的掉落件数」一致，便于校验（例如不允许超过 `dropCount` 条掉落记录）。

### 2.3 实体/表结构建议（关系型或 JSON 皆可）

下面用「表名 + 字段」描述，你用关系型 DB 或文档型（如 JSON 存 MongoDB）都可以等价实现。

**（1）用户与公会成员（在线登录用）**

- **users**（登录账号）  
  - `id`, `username`, `password_hash`, `display_name`, `created_at` 等。  
  - 用于「在线登录」、权限（团长/成员）。
- **members**（公会成员，参与分配的对象）  
  - `id`, `guild_id`（若多公会）, `user_id`（可空，关联登录账号）, `display_name`, `wow_class`, `status`（active/backup 等）, `is_leader` 等。  
  - 一个 user 可以对应一个 member，或仅团长维护成员列表、不绑定账号。

**（2）Raid 日程（日历上的「一天」一次记录）**

- **raid_schedules**（日程）  
  - `id`  
  - `date`（日期，如 2025-03-20；用于日历展示与算周）  
  - `week_id`（可由 date 推导，如 `"2025-W12"`）  
  - `guild_id`（若多公会）, `note`（可选备注）, `created_at`, `created_by`  

- **schedule_raids**（某日程选了哪些团本 + 难度）  
  - `id`  
  - `schedule_id`（属于哪条日程）  
  - `raid_id`（团本 id，对应装备手册）  
  - `difficulty`: `'normal' | 'heroic' | 'mythic'`  
  - 同一日程可多行：例如同一天打了「虚影尖塔-普通」和「梦魇裂口-英雄」就两条。

**（3）Boss 击杀（某日程、某团本某难度下，击杀了某个 Boss）**

- **raid_kills**（或 boss_kills）  
  - `id`  
  - `schedule_id`（属于哪条日程；周与日期从日程带出）  
  - `raid_id`, `boss_id`（对应装备手册里的副本/Boss）  
  - `difficulty`: `'normal' | 'heroic' | 'mythic'`  
  - `participant_count`（参与人数；史诗可固定 20）  
  - `drop_count`（本次击杀应有掉落件数；史诗 4，弹性由规则算出或团长填写）  
  - `created_at`, `created_by`（谁录入的）

**（4）装备掉落记录（单件掉落）**

- **drops**（或 loot_drops）  
  - `id`  
  - `raid_kill_id`（属于哪次 Boss 击杀）  
  - `item_id`（对应装备手册的装备 id）  
  - 可冗余：`item_name`, `slot`, `quality`, `is_tier` 等，便于列表展示  
  - `created_at`  
  - 可选：`bonus_drop`（是否额外掉落，史诗用）

**（5）装备分配记录（谁拿了这件掉落）**

- **distributions**（或 loot_distributions）  
  - `id`  
  - `drop_id`（哪件掉落）  
  - `member_id`（分配给谁）  
  - `distributed_at`, `distributed_by`（操作人，通常为团长）  
  - 可选：`status`（如 `assigned` / `received`，是否已确认领取）

关系链：**raid_schedules → schedule_raids → raid_kills → drops → distributions → members**。  
这样即可实现：  
- **装备掉落记录**：按日程/周/副本/Boss/难度查 `raid_kills`，再查其下 `drops`。  
- **装备分配记录**：查 `distributions`，关联 `drops` 与 `members`，即可看到「谁在哪次击杀拿到了哪件装备」。

---

## 三、主流程交互：日历 → 日程 → 选团本 → 点 Boss → 选掉落 → 分配

下面这一套作为**推荐的主界面与操作顺序**，和上一节的数据模型一一对应。

### 3.1 日历页：记录 Raid 日程

- 使用**日历视图**（按周或按月），每个格子代表一天。
- 用户**点击某一天**：进入该日的「Raid 日程」；若该日尚未创建日程，则新建一条 `raid_schedule`（日期 = 该日），再进入日程详情。
- 在**日程详情**里：
  - **选择团本（可多选）**：从装备手册的副本列表中勾选「今天打了哪些团本」，并可**为每个团本选择难度**（普通/英雄/史诗）。每选一个「团本 + 难度」就写入一条 `schedule_raids`。例如同一天可勾选「虚影尖塔-普通」「虚影尖塔-英雄」「梦魇裂口-英雄」。
  - 保存后，该日程下就有若干「团本 + 难度」组合，用于下一步「选已击杀 Boss」。

### 3.2 在日程里：点选已击杀的 Boss，弹出掉落选择

- 在**同一条日程**下，按「团本 + 难度」分组展示：每个团本下是该副本的 **Boss 列表**（来自装备手册）。
- 用户**单独点击某个 Boss**，表示「这个 Boss 今天击杀了」：
  - 若尚未有该 Boss 的击杀记录，则**新建一条 `raid_kill`**（关联当前日程、该团本、该 Boss、该难度）；并提示或自动带出**参与人数**（弹性时必填，史诗可默认 20），系统算出 **掉落件数** `drop_count`。
  - 点击后**弹出「掉落选择」**：从该 Boss 的**可能掉落列表**（装备手册里该 Boss 的 loot 表）中，让用户**勾选「本次实际掉落的装备」**，数量不超过 `drop_count`（史诗 4 件，可加「额外掉落」）。确认后生成多条 **`drops`**，即**当前 Boss 的掉落列表**。

### 3.3 掉落列表：分配到用户，形成分配记录

- 上一步生成的**掉落列表**在该 Boss 的详情或弹层中展示：每行一件装备，带「分配」操作。
- 团长**逐件点击「分配」**：选择一名**成员**，保存一条 **`distribution`**（关联该 `drop_id` 与 `member_id`），即完成「这件掉落分给谁」的**分配记录**。
- 可展示「已分配 / 未分配」状态，便于团长把当次掉落全部分完。

### 3.4 流程小结（与数据对应）

| 界面操作 | 数据动作 |
|----------|----------|
| 日历上点某日 | 进入/创建 `raid_schedule`（日程） |
| 日程里多选团本并选难度 | 写入多条 `schedule_raids` |
| 点选某 Boss「已击杀」 | 创建 `raid_kill`，并弹出掉落选择 |
| 在 Boss 掉落弹层里从可能掉落中勾选实际掉落 | 创建多条 `drops`（掉落列表） |
| 在掉落列表中为每件选择「分配给谁」 | 为每条 `drop` 创建一条 `distribution`（分配记录） |

这样，**掉落记录** = 某日程下某 Boss 击杀对应的多条 `drops`；**分配记录** = 每条 `drop` 对应的那条 `distribution`。日历负责「哪天打了」和「打了哪些团本」，Boss 点击负责「掉了什么」，分配操作负责「谁拿了」。

---

## 四、在线登录与数据存在哪

- **在线登录**：用户打开网页后，输入账号密码，请求发到**你的后端**；后端校验通过后返回 **Token**（或 Session），前端后续请求都带上该 Token，后端据此识别「当前用户」和权限（团长/成员）。
- **数据存在哪**：  
  - **用户、成员、Boss 击杀、掉落、分配** 全部存在 **服务端数据库**（如 MySQL / PostgreSQL / MongoDB 等）。  
  - 不再依赖浏览器 localStorage 做「唯一数据源」；localStorage 只存 Token 或 SessionID，用于保持登录状态。

这样即可实现：  
- 多设备、多浏览器都能用同一账号登录看到同一批记录；  
- 装备掉落记录与装备分配记录持久化、可查询、可备份。

---

## 五、主要业务流程（与第三节的界面对应）

第三节已描述「日历 → 日程 → 选团本 → 点 Boss → 选掉落 → 分配」的界面流，这里对应成动作与权限即可：

1. **团长：在日历创建/进入日程，选择团本与难度** → 对应 `raid_schedule` + `schedule_raids`。  
2. **团长：在日程里点选已击杀的 Boss** → 创建 `raid_kill`（参与人数、掉落件数由规则或团长确认）；**弹出该 Boss 的可能掉落列表，勾选实际掉落** → 创建多条 `drops`（掉落列表）。  
3. **团长：在掉落列表中为每件选择「分配给谁」** → 为每条 `drop` 创建一条 `distribution`。  
4. **查询与展示**：按日历/周查看日程 → 点某日看团本与 Boss 击杀 → 点某 Boss 看掉落与分配；按成员查「拿到了哪些装备」。  
5. **权限**：团长可创建/编辑日程、击杀、掉落、分配及成员；成员可查看日程、掉落与分配结果及自己的需求与套装进度，不可修改。

---

## 六、技术实现建议（简要）

- **后端**：任意你熟悉的语言（Node、Python、Go 等），提供 REST 或 GraphQL API；负责：注册/登录（发 Token）、成员 CRUD、raid_kills / drops / distributions 的 CRUD、按周/副本/难度/成员查询。  
- **数据库**：关系型（如 PostgreSQL）存 users、members、raid_kills、drops、distributions；若用 JSON 文档库，可把一次击杀 + 其 drops 存成一个文档，distributions 仍可单独成表或嵌入。  
- **前端**：现有 React 项目改为「先调登录接口拿 Token，再所有数据通过 API 读写」；Token 存 localStorage 或 Cookie；页面：登录页、成员管理、Boss 击杀录入、掉落录入、分配操作、按周/副本/成员查看记录。

**与现有项目的关系**：  
- 当前项目是「纯前端 + 本地」；要做「在线登录 + 掉落/分配记录」，需要**新增后端 + 数据库**，前端改为调用 API，并增加**日历页、日程详情、Boss 掉落选择、掉落分配**等页面与流程。  
- 装备手册（副本/Boss/装备列表）可以继续用静态数据或从 Wowhead 等来源定期同步；**掉落记录只存「这次掉了哪件」**，用装备手册的 `item_id` 即可关联。

---

## 七、小结表

| 需求 | 实现要点 |
|------|----------|
| **日历 + 日程** | 日历页点日期 → 日程（`raid_schedule`）；日程上多选团本+难度（`schedule_raids`），再按 Boss 点选击杀与掉落。 |
| 装备**掉落**记录 | 点选已击杀 Boss → 创建 `raid_kill`；从该 Boss 可能掉落中勾选实际掉落 → 多条 `drops`（掉落列表）。 |
| 装备**分配**记录 | 在掉落列表中为每件选「分配给谁」→ 每条 `drop` 对应一条 `distribution`（分配记录）。 |
| 普通/英雄弹性 | 每次击杀存 `participant_count`，用可配置公式或表得到 `drop_count`，再限制本次最多录 N 条掉落。 |
| 史诗固定 | 人数 20、掉落 4 件（+ 可选额外掉落字段）。 |
| 在线登录 | 后端用户表 + 登录接口发 Token；数据全部存服务端 DB；前端带 Token 调 API。 |
| 按周区分 CD | 日程带 `date`/`week_id`，击杀挂在日程下，查询/统计按周过滤。 |

按上述模型即可实现「日历记录 Raid 日程 → 选团本 → 点 Boss 选掉落 → 生成掉落列表并分配到用户」的完整流程；后续若要加「需求申领」「套装进度自动算」等，只需在现有 members/drops/distributions 上扩展或关联查询即可。
